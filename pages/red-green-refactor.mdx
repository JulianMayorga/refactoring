import Shell from "../components/Shell";

export default ({ children }) => <Shell>{children}</Shell>;

# Red, green, refactor

## Red

```js codesandbox=test
function sum(numbers) {
  return 0;
}

it("should return the total of an array of numbers", () => {
  expect(sum([1, 1])).toBe(2);
});
```

## Green

```js codesandbox=test
function sum(numbers) {
  return numbers.reduce((partialSum, currentNumber) => {
    return partialSum + currentNumber;
  }, 0);
}

it("should return the total of an array of numbers", () => {
  expect(sum([1, 1])).toBe(2);
});
```

## Refactor

```js codesandbox=test
function add(a, b) {
  return a + b;
}

function sum(numbers) {
  return numbers.reduce(add, 0);
}

it("should return the total of an array of numbers", () => {
  expect(sum([1, 1])).toBe(2);
});
```
